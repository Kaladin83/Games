<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:gravity="center"
    android:focusableInTouchMode="false"
    android:layout_height="wrap_content">

    <TextView
        style="@style/DialogTitleTextStyle"
        android:id="@+id/nameToSave"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:labelFor="@+id/saveNameEdit"
        android:text="@string/name_to_save"/>

    <EditText
        android:id="@+id/saveNameEdit"
        android:inputType="textVisiblePassword"
        android:fontFamily="@font/news_cycle"
        android:textSize="18sp"
        android:autofillHints="@string/save_name_hint"
        android:paddingHorizontal="10dp"
        android:paddingVertical="5dp"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="10dp"
        android:layout_width="match_parent"
        android:layout_height="40dp"/>

    <include layout="@layout/dialog_2_buttons_layout"/>

</LinearLayout>